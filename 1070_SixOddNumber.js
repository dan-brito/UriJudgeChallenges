/*const lines = prompt("Digite os valores de entrada:");
const X = Number(lines);*/

const X = 8

let count = 0;
let odds = [];
while(odds.length < 6) {
    if((X + count) % 2 !== 0){
    odd = X + count;
    console.log(odd);
    odds.push(odd);
    count ++ ;
} else{
  count ++;
}}

